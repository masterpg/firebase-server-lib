#--------------------------------------------------
#  Storage
#--------------------------------------------------

type Mutation {
  removeStorageDir(key: StorageNodeGetKeyInput!): Boolean!
  moveStorageDir(fromDirPath: String!, toDirPath: String!): Boolean!
  renameStorageDir(dirPath: String!, newName: String!): Boolean!
}
