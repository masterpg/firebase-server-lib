#--------------------------------------------------
#  Storage
#--------------------------------------------------

type Mutation {
  removeStorageDir(key: StorageNodeGetKeyInput!): Boolean!
  moveStorageDir(input: MoveStorageDirInput!): Boolean!
  renameStorageDir(input: RenameStorageDirInput!): Boolean!
}
